# Claude Adapter

Install the canonical skill folder into Claude Code:

```bash
./install.sh claude
```

Destination:

```text
~/.claude/skills/attract-signal
```

Use the same `skills/media/attract-signal/SKILL.md` and bundled scripts as every other agent. `CLAUDE.md` provides repo-level guidance for Claude Code.
