# Hermes Adapter

Install the canonical skill folder into Hermes:

```bash
./install.sh hermes
```

Destination:

```text
~/.hermes/skills/media/attract-signal
```

Use the same `skills/media/attract-signal/SKILL.md` and bundled scripts as every other agent.
